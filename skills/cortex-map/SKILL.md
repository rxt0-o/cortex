---
name: cortex-map
description: Project architecture map — modules, layers, files
---
Call cortex_get_map. If user provides a module name, pass it as parameter.
Present the architecture in a readable format with clear layer separation.
Then ask: "Want dependency analysis for a specific file? (/cortex-deps <file>)"
