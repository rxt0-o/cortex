{
  "name": "project-cortex",
  "version": "0.4.0",
  "description": "Intelligent project memory for Claude Code — remembers, learns, and protects.",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc && npm run bundle",
    "bundle": "esbuild dist/index.js --bundle --platform=node --format=esm --outfile=dist/bundle.js --external:node:* --external:@huggingface/transformers --external:onnxruntime-node",
    "dev": "tsc --watch",
    "start": "node dist/bundle.js"
  },
  "dependencies": {
    "@huggingface/transformers": "^3.8.1",
    "@modelcontextprotocol/sdk": "^1.0.0",
    "sqlite-vec": "0.1.7-alpha.2"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "esbuild": "^0.27.3",
    "typescript": "^5.7.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
