# Cortex DB (pro Projekt)
**/.claude/cortex.db
**/.claude/cortex.db-shm
**/.claude/cortex.db-wal
**/.claude/cortex-daemon.pid
**/.claude/cortex-events.jsonl
**/.claude/cortex-feedback.jsonl
**/.claude/cortex-daemon.heartbeat
**/.claude/cortex-watcher.pid

# Claude Code local settings
.claude/settings.local.json
server/.claude/

# Claude Code project files (local only)
CLAUDE.md
agents/

# Internal plans and design docs
docs/plans/

# Dependencies
node_modules/

# TypeScript Build Output (außer pre-built dist/)
# (dist/ wird committed für Zero-Build-Installation)
dist/
!server/dist/

package-lock.json

# sqlite-vec native extension (local install)
server/native/*.dll
